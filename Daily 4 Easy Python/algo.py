import random

""" 
Generates a random password based
on the length.

Input: pLength, an integer.
Returns: string, the random password.
"""
def randomPassWord(pLength):

    password = "hello"

    # Creates a random password
    # for the specified length.
    for i in range(input("What is the length of the password? "):
        password.join(\
       chr(random.randint(33,126)))
        print(i)


    print(password)
    return password